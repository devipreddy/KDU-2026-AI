# Backend

FastAPI service for the healthcare semantic search platform.

This scaffold exposes:

- `GET /health`
- `GET /api/v1/health`
- `GET /api/v1/config`

Feature commits will add authentication, ingestion, OCR, PHI handling, hybrid retrieval, audit logging, and benchmarks.
